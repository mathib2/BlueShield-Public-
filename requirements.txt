# BlueShield runtime dependencies
# Tested on Raspberry Pi 3B+ / Pi 4 / Pi 5 with Debian 12 (Bookworm),
# Python 3.11. install.sh handles all of this automatically.

# ── Core BLE stack ──
bleak>=0.21.0           # Cross-platform BLE scanner (Windows/Linux/macOS)
dbus-fast>=2.0.0        # Bleak's BlueZ transport — included for explicitness
pyserial>=3.5           # nRF52840 sniffer + WHAD serial transport
bluetooth-numbers>=1.0.0  # SIG company/service id reference (optional fallback)

# ── Sniffer + injection (WHAD nRF52840 ButteRFly firmware) ──
whad>=1.2.0             # Romain Cayre's BLE sniff/inject framework
scapy>=2.5.0            # Packet crafting + parsing (BLE PDUs, SMP, etc.)

# ── Web dashboard ──
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.36.0        # Async socket backend for Flask-SocketIO
python-socketio>=5.10.0
python-engineio>=4.5.0

# ── Evidence integrity (Ed25519 audit log signing) ──
cryptography>=42.0.0

# ── Windows-only (only used when running --terminal on Windows) ──
windows-curses>=2.3.1; sys_platform == "win32"
